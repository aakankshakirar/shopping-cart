<ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center bg-white text-primary border">
      <a href="{{ path('list_prdouct') }}">All</a>
    </li>
  {% for category in categories %}
    <li class="list-group-item d-flex justify-content-between align-items-center bg-white text-primary border">
      <a href="{{ path('list_prdouct', {"category_id": category.id}) }}">{{ category.title }}</a>
    </li>
  {% endfor %}
</ul>
